# OS junk
.DS_Store
Thumbs.db
*~

# Editor
.idea/
.vscode/
*.swp
*.swo

# Node (frontend dev deps for the test runner)
node_modules/
package-lock.json.bak

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/

# Local env / secrets (per-machine config, never commit)
.env
.env.local
.env.*.local

# Personal deploy commands (per-machine, may contain hostnames/paths)
DEPLOY.local.md
*.local.md

# Render outputs (generated, large, regenerable)
*.ppm
frontend/render-*.ppm

# Build / deployment bundles (generated by jetson/scripts/build.sh)
jetson/build/
jetson/renders/

# Ad-hoc debug scripts (kept out of source; recreate when needed)
frontend/tr-bruteforce.mjs

# Helm release artefacts
helm/**/charts/
helm/**/*.tgz

# Docker
docker-compose.override.yml

# macOS
.AppleDouble
.LSOverride
Icon?
