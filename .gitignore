# OS junk
.DS_Store
Thumbs.db
*~

# Editor
.idea/
.vscode/
*.swp
*.swo

# Node (frontend dev deps for the test runner)
node_modules/
package-lock.json.bak

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/

# Local env / secrets (per-machine config, never commit)
.env
.env.example
.env.local
.env.*.local

# Personal deploy commands (per-machine, may contain hostnames/paths)
DEPLOY.local.md
*.local.md

# Local-only scratchpad / project memory. Public repo — keep session
# narrative and personal infrastructure notes out of git.
MEMORY.md

# Personal deploy tooling (rsync targets, env loader, one-off render scripts).
# Public repo — keep deployment specifics local.
scripts/

# Render outputs (generated, large, regenerable)
*.ppm

# Ad-hoc debug scripts (kept out of source; recreate when needed)
tr-bruteforce.mjs

# macOS
.AppleDouble
.LSOverride
Icon?
